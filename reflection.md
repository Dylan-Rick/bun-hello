1. Node_Modules are useful for extra storage so it is less crowded for running the package.json. The node modules contains or houses the package.json.

2. Backticks are useful to make templates with string, multi-line, and tagged literals. 

3. It auto formats when you save it and there are no errors. 

4. I made 2 commits, and it's better with small commits more than big commits because of its simplicity, clarity, and flexibility. 

5. Const is a variable that shouldn't change, block-scope can't reassign and let is a variable that can update but not reassign, block-scope allows reassignment and var is a variable that can update with the same scope, function scope allows reassign.
